<!--
  TEMPLATE — copy to `_news/announcement_N.md` (or any descriptive name).
  Jekyll skips this file (excluded in `_config.yml`).
  News items appear on the about page.

  Two flavors:
    inline: true   → single-line entry rendered directly on /about/
    inline: false  → full post with its own page (needs `title` and body)
-->

---

layout: post
date: 2026-01-01 09:00:00-0400
inline: true
related_posts: false

# title: Optional title (required when inline: false)

---

A short inline announcement. Markdown and :emoji: work. Remove this body and add a `title` when using `inline: false`.
